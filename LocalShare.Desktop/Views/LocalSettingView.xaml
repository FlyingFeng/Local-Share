<UserControl x:Class="LocalShare.Desktop.Views.LocalSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LocalShare.Desktop.Views"
             Unloaded="UserControl_Unloaded"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="1">
                <StackPanel Orientation="Vertical">
                    <Rectangle Height="10"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="监听端口：" VerticalAlignment="Center" Width="200"/>
                        <TextBox Width="280" VerticalContentAlignment="Center"/>
                    </StackPanel>
                    <Rectangle Height="10"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="广播端口：" VerticalAlignment="Center" Width="200"/>
                        <TextBox Width="280" VerticalContentAlignment="Center"/>
                    </StackPanel>
                    <Rectangle Height="10"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="最大同时发送节点数：" Width="200" VerticalAlignment="Center"/>
                        <TextBox Width="280" VerticalContentAlignment="Center"/>
                    </StackPanel>
                    <Rectangle Height="10"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="单一节点最大同时发送文件数：" Width="200" VerticalAlignment="Center"/>
                        <TextBox Width="280" VerticalContentAlignment="Center"/>
                    </StackPanel>
                    <Rectangle Height="10"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock VerticalAlignment="Center" Text="文件保存路径：" Width="200"/>
                        <TextBox Width="280" VerticalContentAlignment="Center"/>
                        <Rectangle Width="10"/>
                        <Button Width="100">选择</Button>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
